package expo.modules.alarmclock

import android.app.Activity

/**
 * Task 6 자리표시자.
 *
 * 잠금화면/화면꺼짐 상태에서 AlarmRingingService의 full-screen intent로 뜨는 전체화면 알람 UI.
 * Task 5는 AlarmRingingService가 컴파일되도록 최소 골격만 제공하고, Task 6에서
 * 실제 잠금화면 UI(끄기/스누즈 버튼, 창 플래그, 매니페스트 등록 등)로 교체한다.
 */
class AlarmActivity : Activity()
