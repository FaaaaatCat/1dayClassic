<?xml version="1.0" encoding="utf-8"?>
<!-- 보조 버튼 '5분 후 다시 알림' — 사진 위에 얹히는 반투명 유리 느낌. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="16dp" />
    <solid android:color="@color/alarm_button_fill" />
    <stroke
        android:width="1dp"
        android:color="@color/alarm_button_stroke" />
</shape>
